In your repository, create a new file called README.md.

Write a short guide:

What the app does (moisture content testing, pan management, history plotting).

How to run it on desktop (open Python and run main.py).

How to build for Android (see next step).

Save and commit the README.
